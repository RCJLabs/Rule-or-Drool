Deliberately broken content root for `tests/validate/root.test.ts`. Every file here violates
at least one validator rule on purpose; the test asserts the expected error codes appear.
Do not "fix" it.
